// ls - list directory contents

#include <stdio.h>
#include <dirent.h>

void list_files( char* folder );

int main(int argc, char** argv) {
  // list_files( folder );
}

void list_files( char* folder) {
  printf("%s", folder);
}

